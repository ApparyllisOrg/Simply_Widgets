name: simply_widgets
description: Widgets created for Apparyllis
version: 1.0.0

publish_to: none

environment:
  sdk: ">=2.14.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  styled_widget: ^0.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
